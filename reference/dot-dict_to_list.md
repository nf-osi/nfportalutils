# Convert a flat Python Dict to R list

An internal function used to convert Annotations objects returned by
`get_annotations`.

## Usage

``` r
.dict_to_list(dict)
```

## Arguments

- dict:

  A flat Python Dict object.
