# Format a test fail message.

Format a test fail message.

## Usage

``` r
test_failed(display_string)
```

## Arguments

- display_string:

  A character string to format as a test failed message.

## Value

A message to the console
