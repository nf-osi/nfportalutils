# Subset with selected data elements

Warns if some elements present will be excluded.

## Usage

``` r
with_selected_elements(df, selectable_elements)
```

## Arguments

- df:

  Data.

- selectable_elements:

  Selectable elements.
