# Walk through a directory

For now, an internal util imported from `synapseutils`.

## Usage

``` r
walk(syn_id, as_list = TRUE)
```

## Arguments

- syn_id:

  Synapse id of directory root to traverse.

- as_list:

  Whether to return as R list.

## Value

An R list or Py generator object.
