# Get publication metadata from PubMed

Get publication metadata from PubMed

## Usage

``` r
from_pubmed(pmid)
```

## Arguments

- pmid:

  PubMed id.

## Value

If PMID found, return meta as table w/ `title` `journal` `author` `year`
`pmid` `doi`.
