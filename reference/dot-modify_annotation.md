# Modify a single annotation on a single file

Modifies a single annotation value on a single (existing) synapse file.

## Usage

``` r
.modify_annotation(synapse_id, key, value)
```

## Arguments

- synapse_id:

  A synapse entity id.

- key:

  The key of the annotation to modify.

- value:

  The value to change the annotation to.
