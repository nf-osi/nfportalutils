# Provide a pass/fail summary result

Provide a pass/fail summary result

## Usage

``` r
manifest_passed(result)
```

## Arguments

- result:

  Result list data from schematic service.

## Value

Boolean for whether passed.

List of structure `list(result = result, notes = notes)`, where `result`
indicates whether the dataset passed.
