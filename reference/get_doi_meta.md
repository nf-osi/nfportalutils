# Get DOI metadata if it exists

Returns list of metadata associated with DOI if exists, otherwise NULL.
Currently usable for certain entity types like files or datasets, though
this should be revised to make more useful with other objects. Note:
Internal/experimental use only, not for production use.

## Usage

``` r
get_doi_meta(id)
```

## Arguments

- id:

  Dataset data.
