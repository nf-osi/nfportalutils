# Wrapper to create Data Sharing Plan to project dataset comparison chart

Wrapper to create Data Sharing Plan to project dataset comparison chart

## Usage

``` r
dsp_dataset_mapping(dsp_datasets, project_datasets)
```

## Arguments

- dsp_datasets:

  Named vector of datasets in data sharing plan.

- project_datasets:

  Named vector of datasets in project.
