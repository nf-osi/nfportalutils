# Helpers ———————————————————————# Add meta for new studies as annotations

Put *selected* metadata into Synapse annotations for the study project
entity.

## Usage

``` r
add_new_study_meta(id, study_meta)
```

## Arguments

- id:

  Id of container representing the study, usually a Synapse project.

- study_meta:

  Study meta as a list.
