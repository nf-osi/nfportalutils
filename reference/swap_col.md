# Swap out old column for a new column in a schema

Swap out old column for a new column in a schema

## Usage

``` r
swap_col(schema, old, new)
```

## Arguments

- schema:

  A table schema.

- old:

  Id of old col.

- new:

  Id of new col.
