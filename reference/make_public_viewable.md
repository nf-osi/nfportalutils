# Set public access to VIEW (READ) only for an entity

Set both registered users and non-registered users to have VIEW-only
permissions. See codelinkmake_public for more permissive permissions to
download (for registered users), which is usually set later at data
release time.

## Usage

``` r
make_public_viewable(id)
```

## Arguments

- id:

  Synapse entity id.
