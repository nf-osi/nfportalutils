# Add to scope

Convenience function to add container to view scope.

## Usage

``` r
add_to_scope(view_id, container_id)
```

## Arguments

- view_id:

  Id of view

- container_id:

  Id of container to add.
