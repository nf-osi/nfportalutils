# Extract Synapse id from URI

Given some vector `x` of URIs/file paths, will try different methods to
extract what's likely the needed Synapse id, then sanity checks of
results.

## Usage

``` r
extract_syn_id_from_ss(x)
```

## See also

[`bare_syn_id()`](bare_syn_id.md)
