% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{validate_dataset_folder}
\alias{validate_dataset_folder}
\title{Validate dataset folder}
\usage{
validate_dataset_folder(id, fileview, mc.cores = NULL)
}
\arguments{
\item{id}{Synapse id of a dataset folder}

\item{fileview}{Fileview to use for resolving folder items (required).
Must have the \code{path} column enabled to support recursive file discovery.}

\item{mc.cores}{Number of cores to use for parallel validation.
Defaults to \code{min(parallel::detectCores() / 2, 4)} to be conservative.
Set to 1 to disable parallel processing.}
}
\value{
A list with \code{validation_error} component containing any validation errors found for files.
}
\description{
Convenience function to validate a dataset folder.
This will use the schema bound to the dataset for validation.
If no schema is bound, this will give an error.
Files inherit the schema binding from the parent folder, so only the folder binding is checked.
}
